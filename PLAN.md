# 📋 BEAMLINE TOOLKIT — 사이트 개발 계획서

> **작성일:** 2026-08-08  
> **문서 범위:** 웹사이트 자체(프론트엔드)만 다룸  
> **호스팅:** GitHub Pages (정적 사이트) + Dynu 무료 도메인

---

## 1. 프로젝트 개요

| 항목 | 내용 |
|:---|:---|
| **사이트 정의** | 빔라인 연구원을 위한 계산기 + 실험 노트 + 로그북 웹 도구 |
| **핵심 가치** | "실험 중 빠르게 계산하고, 기록하고, 다시 찾아보는" 단일 사이트 |
| **호스팅** | GitHub Pages (정적 파일만 서빙) |
| **도메인** | Dynu 무료 도메인 (CNAME 연결) |
| **데이터 저장** | 브라우저 `localStorage`만 사용 (서버 DB 없음) |

---

## 2. 범위 정의

### ✅ 이 계획서에 포함 (사이트 자체 기능)

- 모든 계산기 (광학, 빔라인 물리량)
- 실험 노트, 체크리스트, 샘플 관리
- 로그북 (수동 기록, 태그, 히스토리, 내보내기)
- 칸반 보드, 참고 자료, 설정

### ❌ 이 계획서에서 제외 (별도 프로젝트)

| 제외 대상 | 이유 |
|:---|:---|
| SPEC 파일 모니터링 | 사이트 외부(로컬) 시스템 |
| Google Sheets 연동 | 외부 서비스 |
| 실시간 데이터 수신 (API) | 정적 사이트에서 불가 |
| 파일 업로드/공유 자료실 | 서버 필요, 정적 사이트 불가 |

> 위 항목들은 별도의 로컬 스크립트/서비스로 개발하며, 이 사이트와 직접 연결하지 않는다.

---

## 3. 기술 환경 및 제약

### 3.1 구형 OS / 브라우저 대응

**최소 지원 대상:** CentOS 7 기본 브라우저 (Firefox 60 ESR / Chrome 60~70)

| 항목 | 사용 여부 | 비고 |
|:---|:---|:---|
| `const` / `let` / 화살표 함수 | ✅ 사용 | Firefox 54+, Chrome 49+ 지원 |
| 템플릿 리터럴 | ✅ 사용 | Firefox 34+, Chrome 41+ 지원 |
| `fetch` API | ⚠️ 제한 사용 | 가능하면 피하고 인라인 데이터 활용 |
| 옵셔널 체이닝 `?.` / `??` | ❌ 금지 | 구형 브라우저 미지원 |
| `async/await` | ⚠️ 제한 사용 | Firefox 52+, Chrome 55+ 지원 |
| CSS Flexbox | ✅ 사용 | Firefox 28+ 지원 |
| CSS Grid | ❌ 금지 | 구형에서 부분 지원, 불안정 |
| CSS 변수 | ✅ 사용 | Firefox 31+, Chrome 49+ 지원 |
| `backdrop-filter` | ❌ 금지 | 미지원 |
| CSS `gap` (flex) | ❌ 금지 | 구형 미지원, `margin`으로 대체 |
| `box-shadow` | ⚠️ 최소화 | 과다 사용 시 프레임 드랍 |
| 웹폰트 | ❌ 금지 | 시스템 폰트 사용 |
| localStorage | ✅ 사용 | Firefox 3.5+ 지원 |

### 3.2 GitHub Pages 정적 사이트 대응

| 항목 | 대응 |
|:---|:---|
| 서버 사이드 코드 불가 | 모든 로직을 클라이언트 JS로 구현 |
| 라우팅 | 해시(`#`) 기반 라우팅 사용 |
| 데이터 파일 | JSON을 `fetch`로 읽지 않고, **JS 파일에 인라인**으로 포함 |
| HTTPS | GitHub Pages 자동 제공 |
| 용량 | 정적 파일 총합 100MB 이내 (충분함) |

> **핵심 원칙:** `fetch`로 외부/로컬 데이터를 불러오는 구조를 피하고, 필요한 데이터(d-spacing, 재료 상수 등)는 JS 파일에 상수로 내장한다. 구형 브라우저의 fetch 불안정 문제를 원천 차단한다.

---

## 4. 설계 원칙

| 원칙 | 내용 |
|:---|:---|
| **단일 목적 페이지** | 각 계산기는 독립적으로 즉시 동작 |
| **무의존성** | 외부 라이브러리 0개, 순수 HTML/CSS/JS |
| **데이터 내장** | 계산에 필요한 상수는 JS 파일에 포함 |
| **저장은 localStorage** | 노트/로그/설정은 브라우저에 저장 |
| **점진적 동작** | JS 비활성화 시에도 폼은 표시 (`<noscript>` 안내) |

---

## 5. 디자인 컨셉: Clean Lab Dashboard

| 요소 | 규칙 |
|:---|:---|
| 배경 | 연한 회색 `#f4f5f7` |
| 카드 | 흰 배경 + 1px 테두리, 둥글기 4px |
| 포인트 컬러 | 상태 색상만 사용 (초록/주황/빨강) |
| 수치 폰트 | `Consolas, "Courier New", monospace` |
| 본문 폰트 | 시스템 고딕 (`Arial`, `Helvetica`) |
| 둥글기 | 버튼 2px, 카드 4px (과도한 라운딩 금지) |
| 다크 모드 | CSS 변수 토글로 지원 |

### 색상 팔레트

```css
--bg:        #f4f5f7;
--card:      #ffffff;
--border:    #d1d5da;
--text:      #24292e;
--text-sub:  #586069;
--green:     #28a745;   /* 정상 */
--orange:    #ffc107;   /* 경고 */
--red:       #dc3545;   /* 정지 */
--blue:      #007bff;   /* 강조 */
```

---

## 6. 사이트 구조 (메뉴)

```text
⚛ BEAMLINE TOOLKIT
│
├── 📊 DASHBOARD          ← 홈: 자주 쓰는 계산기 바로가기 + 계산 히스토리
│
├── 🔬 OPTICS             ← 광학 계산
│   ├── 에너지-파장-주파수 변환
│   ├── 브래그 법칙
│   ├── 회절격자
│   ├── 굴절률 / 투과율
│   ├── 에너지-각도 변환
│   ├── Chi-Phi 보정
│   ├── 흡수 보정
│   ├── 크리티컬 각도
│   └── Q-space 변환
│
├── 📏 BEAMLINE           ← 빔라인 물리량
│   ├── 풋프린트
│   ├── 빔 플럭스
│   ├── 에너지 분해능
│   ├── 각도 분해능
│   ├── CDI/BCDI 샘플링
│   ├── 슬릿 크기
│   └── 열팽창 보정
│
├── 📝 LOGBOOK            ← 실험 기록
│   ├── 로그 추가 / 목록
│   ├── 태그 검색
│   ├── 히스토리 / 롤백
│   └── 내보내기 (CSV/JSON/클립보드)
│
├── 🛠 EXPERIMENT         ← 실험 관리
│   ├── 실험 노트
│   ├── 체크리스트
│   ├── 샘플 리스트
│   ├── DAQ 설정 계산기
│   └── 칸반 보드
│
├── 📚 REFERENCE          ← 참고 자료
│   ├── 단위 변환기
│   ├── 결정면 d-spacing DB
│   ├── 유용한 링크
│   └── 제작자 소개 / 후원
│
└── ⚙ SETTINGS            ← 설정
    ├── 테마 (라이트/다크)
    ├── 계산 히스토리
    ├── 즐겨찾기
    └── 키보드 단축키 안내
```

### 페이지 레이아웃

```text
┌──────────────────────────────────────────────────────────────┐
│  ⚛ BEAMLINE TOOLKIT                          [🌙]  [설정]     │
├────────────┬─────────────────────────────────────────────────┤
│            │                                                 │
│  SIDEBAR   │   MAIN CONTENT                                  │
│  (고정)    │                                                 │
│            │   ┌─────────────┐  ┌─────────────┐             │
│  OPTICS    │   │ 계산기 카드  │  │ 계산기 카드  │             │
│  BEAMLINE  │   └─────────────┘  └─────────────┘             │
│  LOGBOOK   │                                                 │
│  EXPERIMENT│   ┌──────────────────────────────┐             │
│  REFERENCE │   │ 결과 / 계산 과정 / 수식        │             │
│  SETTINGS  │   └──────────────────────────────┘             │
│            │                                                 │
└────────────┴─────────────────────────────────────────────────┘
```

---

## 7. 기능 명세

### 7.1 OPTICS (광학 계산)

| 기능 | 입력 | 출력 |
|:---|:---|:---|
| 에너지-파장-주파수 변환 | eV/keV/nm/Å/Hz 중 1개 | 나머지 전부 |
| 브래그 법칙 | d-spacing, 에너지 | θ 각도 |
| 회절격자 | 격자 상수, 입사각, 파장, order | 회절각, 분산력 |
| 굴절률/투과율 | 재료, 두께, 에너지 | n, k, T, R |
| 에너지-각도 변환 | 기준 에너지, 각도, 목표 에너지 | 변환 각도 |
| Chi-Phi 보정 | chi 변화량, 브래그 각도 | phi 보정량 |
| 흡수 보정 | 재료, 두께, 밀도, 에너지 | 투과율 |
| 크리티컬 각도 | 재료, 에너지 | 전반사 임계각 |
| Q-space 변환 | 에너지, 각도 | Q 값 |

### 7.2 BEAMLINE (빔라인 물리량)

| 기능 | 입력 | 출력 |
|:---|:---|:---|
| 풋프린트 | 입사각, 빔 크기 | 수평/수직 풋프린트 |
| 빔 플럭스 | 저장링 전류, 효율 계수 | 샘플 도달 플럭스 |
| 에너지 분해능 | 결정, 반각 | ΔE/E |
| 각도 분해능 | 픽셀 크기, 거리 | Δθ |
| CDI/BCDI 샘플링 | 픽셀, 샘플, 거리 | 샘플링 레시오, 가능 여부 |
| 슬릿 크기 | 소스 크기, 거리 | 슬릿 간격 |
| 열팽창 보정 | 온도 변화, 재료 | 각도 시프트 |

### 7.3 LOGBOOK (실험 기록)

| 기능 | 설명 |
|:---|:---|
| 로그 추가 | 시간, 샘플, 조건, 메모 입력 |
| 태그 | `#Si001`, `#fail` 등 입력, 클릭 필터링 |
| 히스토리 | 조건 변경 시 스냅샷 저장, 롤백 |
| 내보내기 | CSV / JSON / 클립보드 복사 |
| 검색 | 텍스트 + 태그 조합 검색 |

### 7.4 EXPERIMENT (실험 관리)

| 기능 | 설명 |
|:---|:---|
| 실험 노트 | 자동 저장 (localStorage), 내보내기 |
| 체크리스트 | 항목 저장/불러오기, 완료율 표시 |
| 샘플 리스트 | 샘플명, 재료, 두께, 위치 관리 |
| DAQ 설정 | 샘플링 레이트 → 데이터 크기 계산 |
| 칸반 보드 | 할 일 → 진행 중 → 완료 |

### 7.5 REFERENCE (참고 자료)

| 기능 | 설명 |
|:---|:---|
| 단위 변환기 | Å/nm/μm, eV/keV, mbar/Torr 등 |
| d-spacing DB | Si, Ge, Al₂O₃ 등 결정면 데이터 열람 |
| 유용한 링크 | NIST, CXRO, RefractiveIndex.info 등 |
| 제작자 소개 | 포트폴리오, GitHub, 후원 링크 |

### 7.6 SETTINGS (설정)

| 기능 | 설명 |
|:---|:---|
| 테마 | 라이트 / 다크 전환 |
| 계산 히스토리 | 최근 20개 계산 기록 |
| 즐겨찾기 | 자주 쓰는 계산기 상단 고정 |
| 단축키 안내 | 키보드 단축키 목록 |

---

## 8. 기술 스택

| 구분 | 선택 | 이유 |
|:---|:---|:---|
| 마크업 | HTML5 | 구형 브라우저 최대 호환 |
| 스타일 | CSS3 (Flexbox) | Grid 회피 |
| 스크립트 | Vanilla JS (ES6 제한적) | 무프레임워크, 무번들 |
| 라우팅 | 해시(`#`) 라우팅 | 구형 브라우저 안전 |
| 저장 | localStorage | 서버 불필요 |
| 데이터 | JS 인라인 상수 | fetch 회피 |
| 외부 라이브러리 | 없음 | 의존성 0 |

---

## 9. 파일 구조

```text
beamline-toolkit/
│
├── CNAME                  ← Dynu 도메인
├── index.html             ← 단일 진입점
├── style.css              ← 전역 스타일 + 디자인 시스템
│
├── js/
│   ├── app.js             ← 라우팅, 탭 전환, 공통 유틸
│   ├── data.js            ← d-spacing, 재료 상수 등 내장 데이터
│   ├── optics.js          ← 광학 계산 로직
│   ├── beamline.js        ← 빔라인 계산 로직
│   ├── logbook.js         ← 로그북 (localStorage)
│   ├── experiment.js      ← 노트, 체크리스트, 샘플, DAQ, 칸반
│   ├── reference.js       ← 단위 변환, 링크
│   └── settings.js        ← 테마, 히스토리, 즐겨찾기
│
└── README.md
```

> 모든 데이터는 `js/data.js`에 상수로 포함한다. JSON 파일 fetch 없음.

---

## 10. 단계별 개발 계획

### Phase 0: 프로젝트 셋업

| 작업 | 산출물 |
|:---|:---|
| 저장소 생성, 파일 구조 작성 | 기본 폴더/파일 |
| `index.html` 골격 (사이드바 + 메인) | 레이아웃 |
| `style.css` 디자인 시스템 (색상, 카드, 버튼) | 전역 스타일 |
| 해시 라우팅 구현 (`app.js`) | 탭 전환 동작 |

**검증:** CentOS 7 Firefox에서 레이아웃 깨짐 없음, 탭 전환 정상.

---

### Phase 1: 핵심 계산기 (최우선)

> 가장 자주 쓰는 계산기부터 완성하여 즉시 유용한 사이트로 만든다.

| 작업 | 산출물 |
|:---|:---|
| 에너지-파장-주파수 변환 | `optics.js` |
| 브래그 법칙 | `optics.js` |
| 풋프린트 | `beamline.js` |
| 단위 변환기 | `reference.js` |
| 계산 결과 표시 UI | 카드 + 결과 영역 |

**검증:** 계산 결과를 수동 계산/NIST와 대조하여 정확도 확인.

---

### Phase 2: 광학 계산기 확장

| 작업 | 산출물 |
|:---|:---|
| 회절격자 | `optics.js` |
| 에너지-각도 변환 | `optics.js` |
| Chi-Phi 보정 | `optics.js` |
| 흡수 보정 | `optics.js` |
| 크리티컬 각도 | `optics.js` |
| Q-space 변환 | `optics.js` |
| 재료 상수 데이터 내장 | `data.js` |

**검증:** CXRO 계산기와 교차 검증.

---

### Phase 3: 빔라인 계산기 확장

| 작업 | 산출물 |
|:---|:---|
| 빔 플럭스 | `beamline.js` |
| 에너지 분해능 | `beamline.js` |
| 각도 분해능 | `beamline.js` |
| CDI/BCDI 샘플링 | `beamline.js` |
| 슬릿 크기 | `beamline.js` |
| 열팽창 보정 | `beamline.js` |

**검증:** 수동 계산과 대조.

---

### Phase 4: 실험 보조 기능

| 작업 | 산출물 |
|:---|:---|
| 실험 노트 (자동 저장) | `experiment.js` |
| 체크리스트 | `experiment.js` |
| 샘플 리스트 | `experiment.js` |
| DAQ 설정 계산기 | `experiment.js` |
| d-spacing DB 열람 | `reference.js` + `data.js` |

**검증:** localStorage 저장/불러오기 동작 확인.

---

### Phase 5: 로그북

| 작업 | 산출물 |
|:---|:---|
| 로그 추가/목록 | `logbook.js` |
| 태그 입력/필터링 | `logbook.js` |
| 히스토리/롤백 | `logbook.js` |
| 내보내기 (CSV/JSON/클립보드) | `logbook.js` |
| 검색 | `logbook.js` |

**검증:** 새로고침 후 데이터 유지, 내보내기 파일 정상.

---

### Phase 6: 관리 기능 + 마무리

| 작업 | 산출물 |
|:---|:---|
| 칸반 보드 | `experiment.js` |
| 유용한 링크 | `reference.js` |
| 제작자 소개/후원 | `reference.js` |
| 테마 (다크 모드) | `settings.js` |
| 계산 히스토리 | `settings.js` |
| 즐겨찾기 | `settings.js` |
| 키보드 단축키 | `app.js` |

**검증:** 전 기능 동작 확인.

---

### Phase 7: 배포 및 최적화

| 작업 | 산출물 |
|:---|:---|
| GitHub Pages 배포 | 공개 URL |
| Dynu 도메인 CNAME 연결 | 커스텀 도메인 |
| HTTPS 확인 | 자동 발급 |
| 구형 브라우저 최종 테스트 | 검증 리포트 |
| README 작성 | 사용 가이드 |

**검증:** Dynu 도메인에서 HTTPS 정상 동작, 전 기능 확인.

---

## 11. 품질 기준

### 호환성

| 환경 | 기준 |
|:---|:---|
| CentOS 7 + Firefox 60 ESR | 레이아웃/기능 정상 |
| CentOS 7 + Chrome 60~70 | 레이아웃/기능 정상 |
| 최신 Chrome/Edge | 정상 (상위 호환) |
| JS 비활성화 | `<noscript>` 안내 표시 |

### 성능

| 항목 | 목표 |
|:---|:---|
| 첫 로딩 | < 2초 |
| 계산 응답 | < 100ms |
| 총 페이지 용량 | < 500KB |
| 외부 요청 | 0회 (데이터 인라인) |

### 정확도

| 계산기 | 검증 기준 |
|:---|:---|
| 브래그 법칙 | NIST 표준 데이터 |
| 에너지 변환 | 물리 상수 기준 |
| 투과율 | CXRO 교차 검증 |
| 회절격자 | 교재 예제 |

---

## 12. 리스크 및 대응

| 리스크 | 대응 |
|:---|:---|
| 구형 브라우저 JS 문법 오류 | ES2020+ 문법 금지, 수동 테스트 |
| localStorage 용량 초과 (5MB) | 오래된 데이터 자동 삭제 로직 |
| localStorage 비활성화 | 계산기는 동작, 저장만 비활성 안내 |
| 계산 오차 | 검증된 물리 공식 + 상수 사용 |
| GitHub Pages 라우팅 문제 | 해시 라우팅으로 회피 |

---

## 13. 체크리스트

- [ ] 파일 구조 생성
- [ ] 레이아웃 골격 (사이드바 + 메인)
- [ ] 디자인 시스템 CSS
- [ ] 해시 라우팅
- [ ] Phase 1 핵심 계산기 4종
- [ ] Phase 2 광학 계산기 확장
- [ ] Phase 3 빔라인 계산기 확장
- [ ] Phase 4 실험 보조
- [ ] Phase 5 로그북
- [ ] Phase 6 관리 기능
- [ ] Phase 7 배포 + Dynu 연결
- [ ] 구형 브라우저 최종 검증
